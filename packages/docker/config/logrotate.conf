# Chaos Cypher log rotation — applied daily via the logrotate supervisord program.
# Keeps 7 compressed rotations per service log; prevents unbounded growth of
# /data/logs/ on long-running self-hosted deployments.

/data/logs/*.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
    copytruncate
}
